<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Roboto', sans-serif;
            background: linear-gradient(135deg, #6D83F2 0%, #A08DF0 100%);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            margin: 0;
        }
        .profile-container {
            max-width: 400px;
            width: 100%;
            padding: 40px;
            background-color: #ffffff;
            border-radius: 12px;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
            text-align: center;
        }
        .profile-icon {
            background-color: #A08DF0;
            width: 80px;
            height: 80px;
            border-radius: 50%;
            margin: 0 auto 20px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 36px;
            color: #fff;
        }
        h2 {
            color: #333;
            font-size: 24px;
            margin-bottom: 5px;
            font-weight: 600;
        }
        p {
            color: #666;
            font-size: 14px;
            margin-bottom: 20px;
        }
        .profile-info {
            background-color: #f9f9fb;
            padding: 20px;
            border-radius: 10px;
            text-align: left;
            margin-top: 20px;
        }
        .profile-info div {
            display: flex;
            align-items: center;
            margin-bottom: 10px;
            font-size: 16px;
        }
        .profile-info div span {
            margin-left: 10px;
            color: #333;
        }
        .btn-back {
            background-color: #6D83F2;
            color: #fff;
            padding: 10px 20px;
            font-size: 16px;
            border: none;
            border-radius: 5px;
            margin-top: 20px;
            cursor: pointer;
            transition: background-color 0.3s ease;
            text-decoration: none;
            display: inline-block;
        }
        .btn-back:hover {
            background-color: #5a6fcc;
        }
    </style>
</head>
<body>
    <div class="profile-container">
        <div class="profile-icon">👤</div>
        <h2>CYBR 3410</h2>
        <p>Member Profile</p>

        <div class="profile-info">
            <div><strong>Username:</strong> <span><%= user ? user.username : 'Not found' %></span></div>
            <div><strong>Email:</strong> <span><%= user ? user.email : 'Not available' %></span></div>
            <div><strong>Joined:</strong> <span><%= user ? user.joinedDate : 'Not available' %></span></div>
            <div><strong>About:</strong> <span><%= user ? user.about : 'Not available' %></span></div>
        </div>

        <a href="/search" class="btn-back">← Back to Search</a>
    </div>
</body>
</html>
