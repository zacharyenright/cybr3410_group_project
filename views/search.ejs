<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Search</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Roboto', sans-serif;
            background: linear-gradient(135deg, #6D83F2 0%, #A08DF0 100%);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            margin: 0;
        }
        .search-container {
            max-width: 600px;
            width: 100%;
            padding: 30px 40px;
            background-color: #ffffff;
            border-radius: 12px;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
            text-align: center;
        }
        h2 {
            color: #333;
            font-size: 24px;
            margin-bottom: 10px;
            font-weight: 600;
        }
        p {
            color: #666;
            font-size: 14px;
            margin-bottom: 20px;
        }
        .form-group {
            display: flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 20px;
            border: 1px solid #6D83F2;
            border-radius: 5px;
            padding: 5px;
        }
        .form-control {
            flex-grow: 1;
            border: none;
            outline: none;
            padding: 10px;
            font-size: 16px;
            border-radius: 5px;
        }
        .btn-search {
            background-color: #6D83F2;
            border: none;
            color: #fff;
            padding: 10px 20px;
            font-size: 16px;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }
        .btn-search:hover {
            background-color: #5a6fcc;
        }
        .results-container {
            margin-top: 20px;
            padding: 20px;
            background-color: #f9f9fb;
            border-radius: 10px;
            text-align: center;
        }
        .results-container p {
            color: #666;
            font-size: 16px;
            margin-top: 10px;
        }
        .icon {
            font-size: 48px;
            color: #ccc;
        }
    </style>
</head>
<body>
    <div class="search-container">
        <h2>Search Users</h2>
        <p>Find CYBR 3410 students in your network</p>
        <form action="/search" method="POST">
            <div class="form-group">
                <input type="text" class="form-control" id="query" name="searchQuery" placeholder="Enter your search query" required>
                <button type="submit" class="btn-search">🔍 Search</button>
            </div>
        </form>

        <div class="results-container">
            <h3>Results</h3>
            <div id="results">
                <% if (results.length > 0) { %>
                    <ul class="list-group">
                        <% results.forEach(function(result) { %>
                            <li class="list-group-item"><%= result.username %>: <%= result.profile %></li>
                        <% }); %>
                    </ul>
                <% } else { %>
                    <div class="icon">🔍</div>
                    <p>No results found. Try different search terms.</p>
                <% } %>
            </div>
        </div>
    </div>
</body>
</html>
